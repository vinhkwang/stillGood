<%= form_with(model: product_attachment) do |form| %>
  <% if product_attachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_attachment.errors.count, "error") %> prohibited this product_attachment from being saved:</h2>

      <ul>
        <% product_attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.number_field :product_id %>
  </div>

  <div>
    <%= form.label :product_image, style: "display: block" %>
    <%= form.text_field :product_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
